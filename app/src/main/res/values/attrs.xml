<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="ReservationOptions">
        <attr name="textPadding" format="integer" />
        <attr name="showLineState" format="boolean" />
        <attr name="highLight" format="boolean" />
    </declare-styleable>

    <declare-styleable name="DataUnitOptions">
        <attr name="dataText" format="string" />
        <attr name="unitText" format="string" />
        <attr name="dataFontSize" format="float" />
        <attr name="unitFontSize" format="float" />
    </declare-styleable>

</resources>
